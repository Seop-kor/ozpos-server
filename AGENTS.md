# AGENTS.md — My Workflow for Codex

## 핵심 원칙

- 설계가 가장 중요하다. 각 함수의 역할과 슈도코드가 나올 때까지 구현을 시작하지 않는다.
- 가정을 명확히 하세요. 확실하지 않으면 물어보세요.
- 불분명한 부분이 있으면 그만두세요. 무엇이 헷갈리는지 말하고 물어보세요.
- 여러 해석이 존재한다면 제시하세요. 조용히 선택하지 마세요.
- Test-Driven Development를 기본값으로 사용한다. 테스트 먼저, 항상.
- 설계 문서가 진실의 원천이다. TDD와 리뷰 모두 `docs/design/*.md`를 기준으로 한다.
- 단순함이 목표다. YAGNI를 지킨다.
- 증거로 말한다. 완료를 선언하기 전에 실제 검증 결과를 확인한다.
- git 명령은 절대 실행하지 않는다.

## 기본 워크플로우

모든 기능 추가, 동작 변경, 버그 수정은 아래 순서를 따른다.

1. 요구사항 이해
2. 가정/모호함/해석 분기 정리
3. 설계
4. 함수별 역할 정의
5. 슈도코드 작성
6. 사용자 승인
7. 테스트 설계
8. 구현
9. 검증
10. 리뷰
11. 완료 보고

## 스킬 사용 규칙

Codex에서 관련 스킬이 보이면 먼저 해당 스킬을 사용한다. 명시적으로 호출할 때는 `$designing`, `$test-driven-development`, `$requesting-code-review`, `$receiving-code-review`, `$systematic-debugging`처럼 호출한다.

- 새 기능/컴포넌트/동작 변경 전: `designing`
- 설계 승인 후 구현 시작: `test-driven-development`
- 구현 완료 후: `requesting-code-review`
- 리뷰 피드백 반영 전: `receiving-code-review`
- 원인 불명의 버그/테스트 실패: `systematic-debugging`

## 설계 게이트

구현, 스캐폴딩, 리팩터링, 테스트 작성 전에 다음이 끝나야 한다.

1. 목적, 제약, 성공 기준이 정리되어야 한다.
2. 모호함과 가능한 해석이 제시되어야 한다.
3. 접근법 2~3개와 추천안이 제시되어야 한다.
4. 함수/모듈별 역할이 정의되어야 한다.
5. TDD-ready 슈도코드와 Test Cases가 있어야 한다.
6. 사용자의 승인이 있어야 한다.

## TDD 게이트

- 설계 문서의 각 Unit × Test Case마다 RED → GREEN → REFACTOR를 수행한다.
- RED 단계에서 테스트가 실제로 실패하는 것을 확인한다.
- 테스트 없이 작성한 프로덕션 코드는 삭제한다.
- 설계에 없는 테스트가 필요하면 구현을 멈추고 설계를 업데이트/승인받는다.

## 리뷰 게이트

- Stage 1: Spec Compliance. 설계 문서와 구현/테스트가 일치하는지 확인한다.
- Stage 2: Code Quality. Stage 1 통과 후 DRY, YAGNI, 네이밍, 복잡도, 에러 처리, 타입 안정성을 확인한다.
- Critical/Important 이슈가 있으면 `receiving-code-review` 절차로 처리한다.

## 완료 보고

완료 보고에는 최소한 다음을 포함한다.

- 변경 요약
- 실행한 테스트/검증 명령과 결과
- 남은 리스크 또는 확인하지 못한 사항
